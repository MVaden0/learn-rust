fn main() {
    // rust has similar scalar types to other languages
    let int: u8 = 10;
    let float: f32 = 32.345;
    let boolean: bool = false;
    let character: char = 'a';

    // tuple
    let tuple: (u8, f32) = (8, 34.02);

    // array
    let array = [1, 2, 3];
}
